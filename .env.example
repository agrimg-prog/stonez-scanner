# Stonez Strategy — Environment Variables
# Copy this file to .env and fill in your values
# cp .env.example .env

# ── Telegram (required for phone alerts) ──────────────────────
# 1. Message @BotFather on Telegram → /newbot → copy token
TELEGRAM_BOT_TOKEN=

# 2. Message @userinfobot on Telegram → copy the number
TELEGRAM_CHAT_ID=

# ── No Kite Connect needed ────────────────────────────────────
# Data comes from NSE public API + yfinance (both free, no auth)
